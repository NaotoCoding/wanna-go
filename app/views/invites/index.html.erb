<h4>未回答招待一覧</h4>

<table class="table">
  <thead>
    <tr>
      <th>グループ名</th>
    </tr>
  </thead>
  <tbody>
    <% @unconfirmed_invites.each do |invite| %>
      <%= render invite %>
    <% end %>
  </tbody>
</table>
